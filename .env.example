# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/immaculate_baseball"

# Stripe keys — get from dashboard.stripe.com
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# JWT secret — use a long random string (e.g., openssl rand -base64 32)
JWT_SECRET="change-me-to-a-long-random-string"

# Public base URL — used for Stripe redirect URLs
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
